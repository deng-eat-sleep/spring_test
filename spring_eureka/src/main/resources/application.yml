server:
  port: 7001

spring:
  application:
    name: eureka

eureka:
  client:
    refresh:
      enable: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka/