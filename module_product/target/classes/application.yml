server:
  port: 8002

spring:
  application:
    name: product

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/

feign:
    hystrix:
      enabled: true #开启客户端 hystrix